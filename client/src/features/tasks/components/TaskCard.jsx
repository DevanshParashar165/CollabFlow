export default function TaskCard({ task, canManage, canAssign, onOpen, onEdit, onDelete }) {
  return <article className="bg-slate-800/40 border border-slate-700/50 rounded-xl p-4"><div className="flex items-start justify-between gap-3"><button onClick={onOpen} className="text-left text-white font-medium hover:text-indigo-300 cursor-pointer">{task.title}</button><span className="text-xs text-amber-300">{task.priority}</span></div><p className="text-slate-500 text-xs mt-2 line-clamp-2">{task.description || 'No description'}</p><div className="flex items-center justify-between mt-4 text-xs"><span className="text-slate-400">{task.assignee?.name || 'Unassigned'}</span><span className="text-slate-500">{task.dueDate ? new Date(task.dueDate).toLocaleDateString() : 'No due date'}</span></div>{canManage && <div className="flex gap-3 mt-3 pt-3 border-t border-slate-700/50"><button onClick={onEdit} className="text-indigo-300 text-xs cursor-pointer">Edit</button>{canAssign && <button onClick={onDelete} className="text-red-400 text-xs cursor-pointer">Delete</button>}</div>}</article>;
}